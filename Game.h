#ifndef GAME_H_INCLUDED
#define GAME_H_INCLUDED
#include <iostream>
#include <cstddef>
#include <vector>
#include "Field.h"
#include "Tetromino.h"

/*
class Game{
    public:

        struct Cursor{
            Point()
            : x(0), y(0){
            }

            void setY(unsigned short y){
                this->y = y;
            }
            void setX(unsigned short x){
                this->x = x;
            }
            void getY() const{
                return this->y;
            }
            void getX() const{
                return this->x;
            }

            unsigned short x;
            unsigned short y;
        };

        Game(size_t height, size_t width);

        void print() const;

    private:
        Field field;

        Tetromino *actualBrick;
        Point Cursor; in Arbeit

        bool isGameOver;

};
*/

#endif // GAME_H_INCLUDED
